import { FeatureShell } from '@/testCase Frontend';

export default function TestCaseGenerationLayout({ children }: { children: React.ReactNode }) {
  return <FeatureShell>{children}</FeatureShell>;
}
