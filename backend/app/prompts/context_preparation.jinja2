Normalize the supplied project context without inventing requirements. Preserve every source ID and return JSON only. Restructure tech_stack only when it is not already a structured object.
Input: {{ input }}
