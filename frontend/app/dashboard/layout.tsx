import { FeatureShell } from '@/testCase Frontend/components/FeatureShell';

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <FeatureShell>{children}</FeatureShell>;
}
